type Props = {}

const Install = (props: Props) => {
  return (
    <div className="text-white-400 flex-center font-bold heading2 bg-black-100 w-screen">
        <p><b className="text-gradient_pink-orange">Add to Home Screen</b> the app to delve into the world of future <b className="text-gradient_blue-purple">digital payments.</b></p>
    </div>
  )
}

export default Install