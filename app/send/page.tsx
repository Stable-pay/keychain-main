import { getDataFromId } from '@/components/utils/getDatafromId'
import React from 'react'

type Props = {}

const SendPage = async (props: Props) => {
    const sendOptionsData:any=getDataFromId();
  return (
    // {sendOptionsData.receiverAddress!=='' ? }
    <></>
  )
}

export default SendPage